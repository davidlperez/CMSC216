#include <stdio.h>
#include "term.h"
#include "polynomial.h"

/* (c) Larry Herman, 2023.  You are allowed to use this code yourself, but
   not to provide it to anyone else. */

/* the problem is visible via the command:
 *
 * gcc -E main.c | less
 */

int main(void) {
  Term t;
  Polynomial p;

  /* some code */

  add_term(&p, t);

  /* some code */

  return 0;
}
